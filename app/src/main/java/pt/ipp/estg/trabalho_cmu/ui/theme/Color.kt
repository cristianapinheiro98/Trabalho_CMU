package pt.ipp.estg.trabalho_cmu.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * This file defines the color palette for the application's theme.
 * It includes base colors for both light and dark themes, as well as
 * custom colors used for specific UI elements like ranking trophies.
 */

/**
 * A light, vibrant purple color, typically used as a primary accent in a light theme.
 * Hex: #D0BCFF
 */
val Purple80 = Color(0xFFD0BCFF)

/**
 * A light, muted purple-gray, often used for secondary elements or backgrounds in a light theme.
 * Hex: #CCC2DC
 */
val PurpleGrey80 = Color(0xFFCCC2DC)

/**
 * A light, soft pink color, suitable for tertiary accents or highlights in a light theme.
 * Hex: #EFB8C8
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * A deep, rich purple, typically used as a primary color in a dark theme.
 * Hex: #6650a4
 */
val Purple40 = Color(0xFF6650a4)

/**
 * A dark, muted purple-gray, suitable for surfaces or secondary elements in a dark theme.
 * Hex: #625b71
 */
val PurpleGrey40 = Color(0xFF625b71)

/**
 * A dark, desaturated pink, used for tertiary accents in a dark theme.
 * Hex: #7D5260
 */
val Pink40 = Color(0xFF7D5260)

// Colours to SocialTails ranking's trophies

/**
 * A bright, metallic gold color used for the first-place trophy in the SocialTails ranking.
 * Represents the gold medal.
 * Hex: #FFD700
 */
val YellowTrophy = Color(0xFFFFD700)

/**
 * A neutral gray color, likely representing a silver or second-place trophy in the ranking.
 * Represents the silver medal.
 * Hex: #808080
 */
val GrayTrophy = Color(0xFF808080)

/**
 * A warm, metallic brown color for the third-place trophy in the ranking.
 * Represents the bronze medal.
 * Hex: #CD7F32
 */
val BronzeTrophy = Color(0xFFCD7F32)